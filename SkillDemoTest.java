import static org.junit.Assert.*;
import org.junit.*;

public class SkillDemoTest {
    @Test
    public void addition() {
        assertEquals(2, SkillDemo.add(1,1)); // since 1 + 2 is 3, it expected 2, so fails. 
    }
}

